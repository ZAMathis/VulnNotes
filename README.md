# VulnNotes (README.md WIP)

## Installation & Setup

1. Install dependencies:
```bash
   npm install
```

2. Start the application:
```bash
   npm start
```

The SQLite database (`notes.db`) will be automatically created on first run.